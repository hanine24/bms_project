name: flutter_application_1
description: "A new Flutter project."

# Prévention de la publication accidentelle sur pub.dev
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  percent_indicator: ^4.2.4
  
  latlong2: ^0.9.0
  fl_chart: ^0.63.0 # Conservez cette ligne
  
  
  
  
  syncfusion_flutter_gauges: ^20.1.59

flutter:
  uses-material-design: true

  # Définir les assets de ton application
  assets:
    - assets/bms_daly_image.PNG
    - assets/background.PNG

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
